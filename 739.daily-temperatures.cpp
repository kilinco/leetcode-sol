#include <vector>
class Solution {
public:
	std::vector<int> dailyTemperatures(std::vector<int>& T) {
		for(std::vector<int>::iterator it = T.end(); it != T.begin(); it--)
		{
			
		}
		return T;
    }
};
